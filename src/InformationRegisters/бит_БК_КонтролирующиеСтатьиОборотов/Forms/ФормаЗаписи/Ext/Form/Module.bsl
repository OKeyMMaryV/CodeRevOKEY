
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Запись.ИсходныйКлючЗаписи.Пустой() Тогда 
		 Запись.АктивностьСтроки = Истина;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	Если НЕ Запись.ИсходныйКлючЗаписи.Пустой() Тогда
		ПоказатьПредупреждение(, "Изменение данных может привести к некорректной работе документов бюджетного контроля.");
	КонецЕсли;
	
КонецПроцедуры
