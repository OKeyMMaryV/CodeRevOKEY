#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ПерейтиПросмотрЛогов(Команда)
	
	ТекущиеДанные	=	Элементы.Список.ТекущиеДанные;
	
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;	
	КонецЕсли;
	
	ПараметрыОткрытия	=	Новый Структура("ИдентификаторОперации", ТекущиеДанные.ИдентификаторОперации);
	ОткрытьФорму("Обработка.ок_ПросмотрЛоговОпераций.Форма.Форма", ПараметрыОткрытия);
	
КонецПроцедуры

#КонецОбласти