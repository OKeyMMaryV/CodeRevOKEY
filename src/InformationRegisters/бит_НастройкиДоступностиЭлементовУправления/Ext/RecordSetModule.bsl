#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий
	
Процедура ПередЗаписью(Отказ, Замещение)

	Если ОбменДанными.Загрузка Тогда	
		Возврат;	
	КонецЕсли; 
	
	Заголовок = "Ошибка записи регистра сведений ""Настройки доступности элементов управления"":";

	Для Каждого Строка Из ЭтотОбъект Цикл
		 
		Если Не ЗначениеЗаполнено(Строка.Пользователь) Тогда
			ТекстСообщения = "Значение поля ""Пользователь"" не может быть пустым.";
			бит_ОбщегоНазначения.СообщитьОбОшибке(ТекстСообщения, Отказ, Заголовок,СтатусСообщения.Важное);
    	КонецЕсли;
		
		Если  Не ЗначениеЗаполнено(Строка.ИмяЭлементаУправления) Тогда
			ТекстСообщения 	= "Значение поля ""Имя элемента управления"" не может быть пустым.";
			бит_ОбщегоНазначения.СообщитьОбОшибке(ТекстСообщения, Отказ, Заголовок,СтатусСообщения.Важное);
 		КонецЕсли;
			
	КонецЦикла; 
	
КонецПроцедуры // ПередЗаписью()

#КонецОбласти

#КонецЕсли
