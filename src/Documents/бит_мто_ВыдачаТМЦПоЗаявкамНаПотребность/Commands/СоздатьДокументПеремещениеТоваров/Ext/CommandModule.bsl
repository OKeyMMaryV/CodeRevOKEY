
#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ДокументПеремещение = СоздатьДокументПеремещениеТоваров(ПараметрКоманды);
	
	Если ДокументПеремещение <> Неопределено Тогда
	
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("Ключ", ДокументПеремещение.Ссылка);

		ФормаДокумента = ОткрытьФорму("Документ.ПеремещениеТоваров.ФормаОбъекта", ПараметрыФормы);
		
	КонецЕсли; 
	
КонецПроцедуры

// Функция создает документ "Перемещение товаров". 
// 
&НаСервере
Функция СоздатьДокументПеремещениеТоваров(ПараметрКоманды)

	ДокументНакладная = Документы.бит_мто_ВыдачаТМЦПоЗаявкамНаПотребность.СоздатьДокументПеремещениеТоваров(ПараметрКоманды);
	
	Возврат ДокументНакладная;
	
КонецФункции // СоздатьДокументТребованиеНакладная()

#КонецОбласти