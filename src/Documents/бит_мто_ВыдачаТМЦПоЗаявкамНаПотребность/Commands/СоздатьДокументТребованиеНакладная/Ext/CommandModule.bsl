
#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ДокументНакладная = СоздатьДокументТребованиеНакладная(ПараметрКоманды);
	
	Если ДокументНакладная <> Неопределено Тогда
	
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("Ключ", ДокументНакладная.Ссылка);

		ФормаДокумента = ОткрытьФорму("Документ.ТребованиеНакладная.ФормаОбъекта", ПараметрыФормы);
		
	КонецЕсли; 
	
КонецПроцедуры

// Функция создает документ "Требование-накладная". 
// 
&НаСервере
Функция СоздатьДокументТребованиеНакладная(ПараметрКоманды)

	ДокументНакладная = Документы.бит_мто_ВыдачаТМЦПоЗаявкамНаПотребность.СоздатьДокументТребованиеНакладная(ПараметрКоманды);
	
	Возврат ДокументНакладная;
	
КонецФункции // СоздатьДокументТребованиеНакладная()

#КонецОбласти