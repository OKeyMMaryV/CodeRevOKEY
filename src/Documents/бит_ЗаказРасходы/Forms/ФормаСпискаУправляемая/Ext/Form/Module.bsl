
&НаКлиенте
Процедура ДействияФормыИсторияОтправки(Команда)
	
	Если Элементы.Список.ТекущиеДанные = Неопределено Тогда 
		Возврат;
	КонецЕсли;
	
	П = Новый Структура;
	ОтборСтр = Новый Структура;
	ОтборСтр.Вставить("Заказ", Элементы.Список.ТекущиеДанные.Ссылка);
	П.Вставить("Отбор", ОтборСтр);
	
	ОткрытьФорму("РегистрСведений.бит_ИсторияОтправкиЭлектронныхПисемПоЗаказам.ФормаСписка", П);

КонецПроцедуры
